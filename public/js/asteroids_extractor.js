(function() {
  function extractGameData(gameObject) {
    var gameData = {};
    return gameData;
  }
  if (window) {
    window.AsteroidsExtractor = {
      extractGameData: extractGameData
    };
  }
}());
